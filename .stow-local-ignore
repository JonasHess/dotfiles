# Comments and blank lines are allowed.

RCS
.+,v

CVS
\.\#.+       # CVS conflict files / emacs lock files
\.cvsignore

\.svn
_darcs
\.hg

\.idea
\.git
\.gitignore

.+~          # emacs backup files
\#.*\#       # emacs autosave files

^/README.*
^/LICENSE.*
^/COPYING

# Don't stow .nvmrc into $HOME: it would make the home directory look like a
# Node project and trigger nvm's auto-use (a ~600ms switch) on every shell.
^/\.nvmrc

\.DS_Store